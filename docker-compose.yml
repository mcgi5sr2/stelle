services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: stele
      POSTGRES_PASSWORD: stele
      POSTGRES_DB: stele
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
